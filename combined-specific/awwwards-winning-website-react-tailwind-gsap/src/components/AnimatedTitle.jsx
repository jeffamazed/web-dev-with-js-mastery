const AnimatedTitle = ({ title, containerClass }) => {
  return (
    <p className="mt-5 text-4xl uppercase leading-[0.8] md:text-[6rem]">
      {title}
    </p>
  );
};

export default AnimatedTitle;
